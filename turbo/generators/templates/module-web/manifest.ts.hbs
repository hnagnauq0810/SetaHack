import { type NavManifest, noNavExtensions } from '@seta/module-sdk';
import { Box } from 'lucide-react';

export const {{camelCase name}}NavManifest: NavManifest = {
  id: '{{name}}',
  label: '{{titleCase name}}',
  icon: Box,
  requiredPermissions: [],
  useNavExtensions: noNavExtensions,
  nav: [
    {
      label: '{{titleCase name}}',
      items: [{ id: '{{name}}.home', icon: Box, label: '{{titleCase name}}', to: '/{{name}}' }],
    },
  ],
};
