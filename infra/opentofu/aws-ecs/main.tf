# This file is intentionally a placeholder.
#
# There is no "deploy the root directly" path. Operators pick one of the
# examples under `examples/` (single-service or split-services), copy the
# directory's `terraform.tfvars.example` to `terraform.tfvars`, edit values,
# and run `tofu init && tofu apply` from inside that example directory.
#
# See README.md for the topology overview and decision summary.
