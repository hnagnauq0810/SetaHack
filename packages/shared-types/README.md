# @seta/shared-types

Cross-module type definitions — event payload contracts, branded ID
types (`UserId`, `TenantId`, …), and shared DTOs that cross the
public-surface boundary. Type-only; no runtime code.

## Exports

| Entry | Purpose |
|---|---|
| `@seta/shared-types` | Branded IDs, event payload types, shared DTOs |
